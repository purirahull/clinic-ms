<nav class='bg-secondary p-3 d-flex overflow-scroll w-100 m-0 '>


    <div class='d-flex flex-row justify-content-between w-100'> 
        <div class='d-flex'> 
            <div class='text-white mx-3'><%= link_to "Home" , patients_path , :class=>'text-white'%></div>
            <div class='text-white mx-3'><%= link_to "Patients" , patients_path , :class=>'text-white'%></div>
            
            <% if current_user && current_user.doctor? %> 
                <div class='text-white mx-3'><%= link_to "Stats" , stats_path , :class=>'text-white'%></div>
            <% end %>
            </div>
        <div class='d-flex justify-content-end w-100'>
            <div class='d-flex'>
                <% if current_user.nil? %> 
                    <div class='text-white mx-3'><%= link_to "LogIn" , new_user_session_path ,:class=>'text-white mt-0cu p-0'%></div>
                <% else %>
                    <div class='text-white mx-3'>  <%= current_user && current_user&.full_name.concat(" ",current_user.id.to_s)%></div>  
                    <div class='text-white mx-3'><%= button_to "Log Out" , destroy_user_session_path , method: :delete ,:class=>'text-white btn-outline btn m-0 p-0'%></div> 
                <% end %>
            </div>
        </div>  
    </div>
</nav>
