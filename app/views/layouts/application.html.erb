<!DOCTYPE html>
<html>
  <head>
      <title>Clinic MS</title>
      <meta name="viewport" content="width=device-width,initial-scale=1">
      <%= csrf_meta_tags %>
      <%= csp_meta_tag %>
      <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
      <%= favicon_link_tag  asset_path("logo.jpg") %>
  </head>

  <body>
      <%= render "layouts/nav"  %>
      <div class='container-fluid '>
      <%= yield %>
      </div>
  </body>
</html>
